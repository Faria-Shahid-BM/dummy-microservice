[pytest]
testpaths = tests
# The engines package lives at the repo root, which is not on sys.path when
# pytest is invoked from elsewhere (CI, an IDE runner). rootdir is wherever
# this file is, so this makes `from engines import ...` work either way.
pythonpath = .
